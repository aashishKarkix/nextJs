import {add, divide, multiply, subtract} from "./math";

const addition = add(1, 2);
const subtraction = subtract(1, 2);
const multiplication = multiply(1, 2);
const division = divide(1, 2);

console.log(`addtion: ${addition} subtraction: ${subtraction}, multiplication: ${multiplication}, divide: ${division}`);